REST API
Реализация отправки сообщения получателю.
Отправка на TELEGRAM, SMS, EMAIL, WHATSAPP

При отправке сообщения происходит запись в лог.

Отправка сообщений происходит через сервис Postman.
POST запрос localhost:8080 -> SEND
Body -> row -> JSON -> тело запроса { запрос }

Пример сообщения:
    {
        "id":"1987",
        "userId":"9830102",
        "message":"Передаю привет Авроре",
       "type":"WHATSAPP"
    }

Пример получения:
    Отправляем в WhatsApp: Передаю привет Авроре